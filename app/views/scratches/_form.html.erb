<%= simple_form_for(@scratch) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :team_id %>
    <%= f.association :debater_id %>
    <%= f.input :type %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
