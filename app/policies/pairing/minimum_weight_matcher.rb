# frozen_string_literal: true

# uses the graph_matching gem to generate a minimum-weight matching for the
# passed bracket
class MinimumWeightMatcher
  def initialize(bracket)
    @bracket = bracket
  end
end
